Logos for the homepage "One app instead of all of these" section.
Drop the files in THIS folder with EXACTLY these names:

  excel.png
  google-sheets.png
  whatsapp.webp
  life360.png
  google-calendar.webp
  google-maps.png
  xero.png
  quickbooks.png

Format: square, transparent background, PNG, at least 128x128px.
They render at 26px, so any reasonable size works — square matters most.

Until a file exists, its chip shows a grey initial instead (nothing breaks).
The list of apps lives in components/marketing/ReplacesSection.tsx — add an
entry there if you want more chips.
